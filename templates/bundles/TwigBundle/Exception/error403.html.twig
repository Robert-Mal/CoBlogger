{% extends 'base.html.twig' %}

{% block title %}403{% endblock %}

{% block body %}
    <div class="flex flex-col h-screen w-screen font-poppins">
        <div class="flex items-center gap-5 px-24 py-3">
            <div class="w-8 h-8 rounded-full bg-royal-blue"></div>
            <a href="{{ path('app_dashboard') }}" class="font-semibold text-lg">CoBlogger</a>
        </div>
        <div class="flex flex-col gap-2 h-full w-full justify-center items-center">
            <img src="{{ asset('build/images/403error.252dd384.svg') }}" alt="403 error" class="w-1/3">
            <p class="text-3xl font-semibold">No permission.</p>
            <a href="{{ path('app_dashboard') }}" class="text-royal-blue">Go to dashboard</a>
        </div>
    </div>
{% endblock %}